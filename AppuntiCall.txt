29/06

Qual è l'obiettivo dello sprint0?
Serve per formalizzare i requisiti. 

METTERE LINK AI REQUISITI.

Nodi: 
- virtualrobot
- cargoservice (applicazione) (contiene anche il marker)
- sonar (pico)

Quindi almeno 3 nodi (4 se contiamo l'IOport separata)

marker: container sul marker, aspetta qualche secondo e poi quando ha finito finisce l'operazione

IOPort: qualcosa che mostri al cliente un bottone e un display (webgui). il bottone invia richieste al sistema, il display riceve informazioni. il cliente può accedervi da qualunque dispositivo collegato alla rete su un browser (smartphone). il cliente accede all'ip e si apre la pagina. chi presenta la pagina? lo stesso nodo (dispositivo) che gestisce l'applicazione oppure un'applicazione a parte (ad hoc). (microservizio separato (spoiler: lo preferisce cosi))

E' un sistsema eterogeneo (ogni nodo usa un tipo di software diverso). 
Software sull'applicazione: qak(?) java(?)
A fronte di questi requisiti, in java c'è una carenza. Per comunicare/scambiare messaggi con nodi diversi usiamo qak. (è un'esigenza). non è un linguaggio di programmazione, ma di modellazione. 

virtualrobot è un servizio che il cargorobot sfrutta (non lo estende) (cargorobot specializza il virtualrobot)

cargorobot è un pojo o un service?
il committente ha a disposizione più tipi di virtualrobot (robotsmart, robotobj, robotservice). Si cerca il sw più capace di ridurre l'abstraction gap. E' il robotsmart, che è capace di calcolare il percorso per arrivare a un punto preciso, dando in input solo le coordinate da raggiungere. 

Test: 
- Si prova a caricare un container (core business)




